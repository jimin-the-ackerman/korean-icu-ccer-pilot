# ============================================
# Pilot Configuration - Korean ICU CCER Pipeline
# ============================================

project:
  name: "korean-icu-ccer-pilot"
  stage: "pilot"        # pilot(5) -> full(50) 로 바꿀 때 이 값도 바꿔서 결과 폴더 구분

experiment:
  n_scenarios: 5         # 파일럿: 5, 확장 시: (이 숫자만 바꾸면 됨)
  styles:
    - formal_template
    - clinical_charting
    - telegraphic_icu

generation:
  provider: "openai"
  model: "gpt-4o"
  temperature: 0.7

tts:
  provider: "openai"
  model: "tts-1"
  voice: "alloy"   # 단일 화자 고정 (paper Section 3.8: pilot 단계 speaker 변수 통제)
  output_format: "mp3"

stt:
  provider: "openai"
  model: "whisper-1"
  language: "ko"   # 파일럿: 한국어 고정. Auto Detect 비교는 Future Work로 보류

entity_extraction:
  claude_model: "claude-sonnet-4-6"

paths:
  scenarios_dir: "data/scenarios"
  generated_text_dir: "data/generated_text"
  audio_dir: "data/audio"
  stt_transcripts_dir: "data/stt_transcripts"
  entities_dir: "data/entities"
  results_dir: "results/pilot_15"